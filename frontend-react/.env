# TomTom API key is managed ONLY on the backend (application.yml).
# This frontend has NO API keys — all sensitive calls are proxied through Spring Boot.
# See: backend/src/main/resources/application.yml
VITE_API_BASE=http://localhost:8080
